# Ajo / Contribution Tracker — Python dependencies
# Python 3.11+ required.

Flask==3.1.3
gspread==6.2.1
google-auth==2.55.0
twilio==9.10.9
requests==2.34.2
python-dotenv==1.2.2

# Timezone data — ensures Africa/Lagos resolves via zoneinfo on all platforms.
tzdata==2026.2
